services:
  - type: web
    name: dragon-radio
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 3600 --worker-class sync --workers 4
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
